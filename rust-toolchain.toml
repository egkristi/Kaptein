[toolchain]
# Pinned (not "stable") so the `-D warnings` clippy gate is reproducible across
# contributors and CI. Bump deliberately, not automatically.
channel = "1.97.1"
components = ["rustfmt", "clippy"]
